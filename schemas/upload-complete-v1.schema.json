{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://scalingneuro.com/schemas/upload-complete-v1.schema.json",
  "title": "Scaling Neuro complete-upload request v1",
  "description": "The strict request body for POST /v1/uploads/{upload_id}/complete. Objects are full R2 keys and must exactly match the initialized upload.",
  "type": "object",
  "additionalProperties": false,
  "required": ["objects"],
  "properties": {
    "objects": {
      "type": "array",
      "minItems": 1,
      "maxItems": 64,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["key", "size", "sha256", "parts"],
        "properties": {
          "key": {
            "type": "string",
            "minLength": 1,
            "maxLength": 512,
            "pattern": "^(?!/)(?!.*(?:^|/)\\.{1,2}(?:/|$))(?!.*//)[A-Za-z0-9._~/-]+$"
          },
          "size": { "type": "integer", "minimum": 2, "maximum": 5368709120 },
          "sha256": {
            "$ref": "https://scalingneuro.com/schemas/common-v1.schema.json#/$defs/sha256"
          },
          "parts": {
            "type": "array",
            "minItems": 1,
            "maxItems": 10000,
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": ["part_number", "etag"],
              "properties": {
                "part_number": { "type": "integer", "minimum": 1, "maximum": 10000 },
                "etag": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 256,
                  "pattern": "^[A-Za-z0-9+/=_:.-]+$"
                }
              }
            }
          }
        }
      }
    }
  }
}
